name: Deploy to Azure
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        
<<<<<<< HEAD
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: chessboardArsalan34 # Make sure this perfectly matches your Azure App Name
=======
- name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'chessboardArsalan34' # Must match Azure exactly
>>>>>>> 7686d70eff145eabfb03ae97c462272250be0f02
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .
